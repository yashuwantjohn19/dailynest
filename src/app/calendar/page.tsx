'use client'
import { useEffect, useMemo, useState } from 'react'
import { useRouter } from 'next/navigation'
import Link from 'next/link'
import { ChevronLeft, ChevronRight, Info, Loader2 } from 'lucide-react'
import Navigation from '../../components/Navigation'
import { useUser } from '../../hooks/useUser'
export default function CalendarPage(){const {user,loading}=useUser();const router=useRouter();const [date,setDate]=useState(()=>new Date());useEffect(()=>{if(!loading&&!user)router.replace('/login?next=/calendar')},[loading,user,router]);const days=useMemo(()=>{const y=date.getFullYear(),m=date.getMonth(),offset=new Date(y,m,1).getDay(),count=new Date(y,m+1,0).getDate();return [...Array(offset).fill(null),...Array.from({length:count},(_,i)=>i+1)]},[date]);if(loading||!user)return <div className="app-surface flex items-center justify-center"><Loader2 className="animate-spin text-[#e56b35]"/></div>;const label=date.toLocaleDateString('en-IN',{month:'long',year:'numeric'});return <div className="app-surface"><Navigation/><div className="lg:pl-64"><main className="mx-auto max-w-6xl px-4 py-10 sm:px-8"><header className="border-b border-[#dfd0bd] pb-8"><p className="eyebrow text-[#bb4824]">Delivery rhythm</p><h1 className="editorial-title mt-3 text-5xl font-black sm:text-6xl">Your calendar.</h1><p className="mt-4 max-w-2xl leading-7 text-[#6f625f]">Confirmed server-backed delivery dates will appear here. This preview does not issue wallet charges or refunds when a date is selected.</p></header><section className="mt-8 grid gap-6 lg:grid-cols-[1fr_19rem]"><div className="surface-card min-w-0 p-4 sm:p-7"><div className="flex items-center justify-between border-b border-[#eadfce] pb-5"><h2 className="text-xl font-black">{label}</h2><div className="flex gap-2"><button aria-label="Previous month" className="button button-outline !min-h-10 !p-2" onClick={()=>setDate(new Date(date.getFullYear(),date.getMonth()-1,1))}><ChevronLeft/></button><button aria-label="Next month" className="button button-outline !min-h-10 !p-2" onClick={()=>setDate(new Date(date.getFullYear(),date.getMonth()+1,1))}><ChevronRight/></button></div></div><div className="mt-5 grid grid-cols-7 text-center text-[.65rem] font-black uppercase tracking-wider text-[#6f625f]">{['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map(d=><span key={d}>{d}</span>)}</div><div className="mt-2 grid grid-cols-7 gap-1 sm:gap-2">{days.map((d,i)=>d===null?<span key={`e${i}`}/>:<button key={d} className="aspect-square min-h-10 border border-[#eadfce] bg-[#fffdf8] text-sm font-bold hover:border-[#e56b35] hover:bg-[#fff3df]" aria-label={`${d} ${label}`}>{d}</button>)}</div></div><aside className="surface-card self-start p-6"><Info className="text-[#397354]"/><h2 className="mt-4 text-xl font-black">Schedule controls</h2><p className="mt-3 text-sm leading-6 text-[#6f625f]">Pause, resume, cancellation, and refund eligibility require verified subscription and delivery records. No simulated request is saved from this calendar.</p><Link href="/subscription" className="button button-dark mt-6 w-full">Manage subscription</Link></aside></section></main></div></div>}
