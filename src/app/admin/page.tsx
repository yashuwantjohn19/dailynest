'use client'
import { useEffect, useState } from 'react'
import Link from 'next/link'
import { AlertCircle, Building2, ChefHat, Loader2, Users } from 'lucide-react'
import AdminShell from '../../components/AdminShell'
type Production={apartment_name:string;total_chapatis:number}
export default function AdminPage(){const [rows,setRows]=useState<Production[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{fetch('/api/production/today').then(async r=>{if(!r.ok)throw new Error('Live production forecast is unavailable.');const d=await r.json();setRows(Array.isArray(d)?d:d.production||[])}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <AdminShell eyebrow="DailyNest operations" title="Kitchen overview" description="Authenticated operational data only. Invented fallback totals are never substituted when a service is unavailable."><section className="mt-8 grid gap-5 sm:grid-cols-3">{[[Users,'Customer records','/admin/customers'],[Building2,'Service apartments','/admin/apartments'],[ChefHat,'Production planner','/admin/production']].map(([Icon,label,href])=>{const I=Icon as typeof Users;return <Link key={label as string} href={href as string} className="surface-card p-6 hover:border-[#e56b35]"><I className="text-[#397354]"/><h2 className="mt-6 text-xl font-black">{label as string}</h2><p className="mt-2 text-xs font-bold uppercase tracking-wider text-[#6f625f]">Open workspace →</p></Link>})}</section><section className="mt-10"><p className="eyebrow text-[#bb4824]">Today’s verified forecast</p>{loading?<div className="surface-card mt-4 p-10 text-center"><Loader2 className="mx-auto animate-spin"/></div>:error?<div role="alert" className="mt-4 flex gap-3 border border-red-200 bg-red-50 p-5 text-sm text-red-800"><AlertCircle className="shrink-0"/>{error} No fallback total is shown.</div>:rows.length===0?<div className="surface-card mt-4 p-10 text-center"><ChefHat className="mx-auto text-[#9b8177]"/><h2 className="mt-4 font-black">No production rows returned</h2><p className="mt-2 text-sm text-[#6f625f]">There are no verified orders in today’s response.</p></div>:<div className="mt-4 overflow-x-auto border border-[#dfd0bd] bg-[#fffdf8]"><table className="w-full min-w-[480px] text-sm"><thead className="bg-[#f1e4cf]"><tr><th className="p-4 text-left">Apartment</th><th className="p-4 text-right">Chapatis</th></tr></thead><tbody>{rows.map((r,i)=><tr key={`${r.apartment_name}-${i}`} className="border-t border-[#eadfce]"><td className="p-4 font-bold">{r.apartment_name}</td><td className="p-4 text-right text-2xl font-black">{r.total_chapatis}</td></tr>)}</tbody></table></div>}</section></AdminShell>}
