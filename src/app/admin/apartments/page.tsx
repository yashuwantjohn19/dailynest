'use client'
import { useEffect,useState } from 'react'
import { AlertCircle,Building2,Loader2,MapPin } from 'lucide-react'
import AdminShell from '../../../components/AdminShell'
import { Apartment } from '../../../types/database'
export default function ApartmentsPage(){const [rows,setRows]=useState<Apartment[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{fetch('/api/apartments/list').then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||'Apartment service unavailable.');setRows(d.apartments||[])}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <AdminShell eyebrow="Service map" title="Apartment communities" description="Read-only view of apartments returned by the authorized service. No browser-local additions are presented as registered buildings."><section className="mt-8">{loading?<Loader2 className="animate-spin"/>:error?<div role="alert" className="flex gap-3 border border-red-200 bg-red-50 p-5 text-sm text-red-800"><AlertCircle/>{error}</div>:rows.length===0?<div className="surface-card p-10 text-center"><Building2 className="mx-auto text-[#9b8177]"/><h2 className="mt-4 font-black">No apartments returned</h2></div>:<div className="grid gap-4 md:grid-cols-2">{rows.map(a=><article key={a.id} className="surface-card p-6"><Building2 className="text-[#e56b35]"/><h2 className="mt-5 text-xl font-black">{a.name}</h2><p className="mt-3 flex gap-2 text-sm leading-6 text-[#6f625f]"><MapPin className="shrink-0" size={17}/>{a.address}, {a.city} {a.zip_code}</p></article>)}</div>}</section></AdminShell>}
